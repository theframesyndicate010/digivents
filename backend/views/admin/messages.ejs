<%- include('layout', { 
    title: 'Messages', 
    currentPage: 'messages', 
    pageScript: '/js/messages.js',
    user: typeof user !== 'undefined' ? user : null,
    body: include('partials/messages-content')
}) %>